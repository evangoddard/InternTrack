export default function Footer() {
  return (
    <footer className="mt-auto border-t border-border">
      <div className="mx-auto max-w-6xl px-4 py-6 text-xs text-text-faint sm:px-6">
        Postings from the SimplifyJobs GitHub repo. Always apply on the company&apos;s own site.
      </div>
    </footer>
  );
}
